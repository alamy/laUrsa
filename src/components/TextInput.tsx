import { clsx } from 'clsx';

export interface TextInputProps {
    //disable?: boolean;
}

export function TextInput({/*disable = false*/}: TextInputProps ) {

    return (
        <input
            className={clsx()}
        
            
        />
    );
}